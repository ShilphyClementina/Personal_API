pipeline {
  agent any
  tools {
    nodejs 'NodeJS_18'   // must match the NodeJS tool name you configured in Jenkins
  }
  stages {
    stage('Checkout') {
      steps {
        checkout scm
      }
    }
    stage('Run Newman Tests') {
      steps {
        bat 'npx newman run "Users API.postman_collection.json" -e "TEST1.postman_environment.json"'
      }
    }
  }
}